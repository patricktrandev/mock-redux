import {
  fetchAnswerFailed,
  fetchAnswerStart,
  fetchAnswerSuccess,
} from "./actions";
import { fetchHandler } from "./apiFetch";

export const fetchData = () => {
  return async (dispatch) => {};
};
